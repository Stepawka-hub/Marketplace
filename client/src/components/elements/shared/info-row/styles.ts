import { SxProps } from "@mui/material";

export const containerStyle: SxProps = {
  display: "flex",
  alignItems: "center",
  gap: 1,
} as const;

export const labelStyle: SxProps = {
  minWidth: 60
}

import { FC } from "react";
import { CreateLotButtonUI } from "@/components/elements";

export const CreateLotButton: FC = () => {
  const handleClick = () => {};

  return <CreateLotButtonUI handleAction={handleClick} />;
};

import { SxProps } from "@mui/material";

export const titleStyle: SxProps = {
  fontSize: {
    xs: "1rem",
    sm: "1.05rem",
    md: "1.1rem",
    lg: "1.15rem",
  },
  fontWeight: 500,
  mb: 1,
} as const;

export const buttonStyle: SxProps = {
  mt: 1,
} as const;

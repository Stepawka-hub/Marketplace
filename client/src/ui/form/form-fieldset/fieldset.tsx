import { styled } from "@mui/material";

export const FormFieldset = styled("fieldset")(() => ({
  display: "flex",
  flexDirection: "column",
  gap: "1.25rem",
  border: "none",
  marginTop: "0.5rem",
}));
